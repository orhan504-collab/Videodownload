name: Flutter APK Oluşturucu

on:
  push:
    branches: [ main ] # Kod her güncellendiğinde çalışır

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # v3 -> v4 yaptık
      
      - name: Java Kurulumu
        uses: actions/setup-java@v4 # v3 -> v4 yaptık
        with:
          distribution: 'zulu'
          java-version: '17' # Flutter'ın yeni sürümleri için Java 17 daha iyidir
          
      - name: Flutter Kurulumu
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.x'
          
      - name: Bağımlılıkları Yükle
        run: flutter pub get
        
      - name: APK Derle
        run: flutter build apk --release
        
      - name: APK'yı Kaydet
        uses: actions/upload-artifact@v4 # Hata veren yer burasıydı, v4 yaptık
        with:
          name: uygulama-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          
