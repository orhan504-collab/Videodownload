name: videodownload
description: YouTube Video ve Müzik İndirici
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  youtube_explode_dart: ^2.2.2  
  dio: ^5.4.0                   
  # Full yerine daha stabil olan Video paketine geçtik
  ffmpeg_kit_flutter_video: ^6.0.3 
  path_provider: ^2.1.1         
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
